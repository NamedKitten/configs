if BTC=`curl -sSL https://coinbase.com/api/v1/prices/historical | head -n 1 | sed "s|^.*,|$|" | sed "s|\(\.[0-9]$\)|\10|"`; then
	echo $BTC
	if [ $BTC != `cat $HOME/BTC/tmp` ]; then
		echo `date +[%D]%H:%M` "| $BTC" >> $HOME/BTC/changes.txt	
		echo $BTC > $HOME/BTC/tmp
	fi
else
	echo "$0000.00"
fi
