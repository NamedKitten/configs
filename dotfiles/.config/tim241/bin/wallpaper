#!/usr/bin/env bash
if [ -d "$HOME/.config/tim241/themes/$DESKTOP_THEME/wallpaper/" ]; then
	for wallpaper in $HOME/.config/tim241/themes/$DESKTOP_THEME/wallpaper/*; do
		WP="$WP $wallpaper"
	done
	if [ "x$WP" != "x" ]; then
		feh --bg-fill $WP
	fi
else
	echo Theme wallpaper not found!
fi
